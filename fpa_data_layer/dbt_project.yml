name: 'fpa_data_layer'
version: '1.0.0'
config-version: 2

profile: 'fpa_data_layer'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fpa_data_layer:
    staging:
      +materialized: view
    marts:
      +materialized: table
